import React, { Fragment } from "react";

function Feedback({ headless, handleSubmit }) {
  return (
    <Fragment>
      <input /> <button>Submit Feedback</button>
    </Fragment>
  );
}
export default Feedback;
